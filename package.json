{
  "name": "puhelinluettelo",
  "version": "2.0.0",
  "description": "A fullstack application for 'puhelinluettelo'",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "build:ui": "cd frontend && npm i && npm run build",
    "deploy": "flyctl deploy",
    "deploy:full": "npm run build:ui && flyctl deploy",
    "eslint": "eslint ."
  },
  "author": "leoalho",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mongoose": "^6.6.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.24.0",
    "nodemon": "^2.0.20"
  }
}
